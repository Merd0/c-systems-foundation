# Real-World Usage Map

How repository examples map to industry-style tasks:

- `sales_report_generator.c`:
  - E-commerce / ERP reporting logic
  - Collect and format data with arrays + structs + functions
- `user_role_access_simulation.c`:
  - Basic authorization (RBAC) ideas
  - Uses `if-else`, `strcmp`, `bool`, and struct pointers
- `mini_atm_simulation.c`:
  - Menu-driven terminal app pattern
  - Loop + switch + input validation
- `student_stats_dashboard.c`:
  - Basic analytics and KPI calculations
  - Produces average/min/max style metrics

Goal: move beyond syntax and answer “where is this used in real projects?”
